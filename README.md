# BlaseIt

_Because doing stuff manually is a chore_

## tl;dr

Get your `connect.sid` cookie by inspecting your browser traffic on <https://blaseball.com> after logging in, add it to a file named `.cookie`. Then:

```bash
$ pip install -r requirements.txt
$ ./main.py
```
